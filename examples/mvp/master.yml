---
# Structure of the MVP.
imports:
  - client
  - server

structure:
  apps:
    calculator:
      type: app
      components:
        client:
          providers: client
          type: app.client

        server:
          providers: server
          type: app.server
          builder: 'falcon'
