---
# Structure of the MVP.
imports:
  - client
  - server

structure:
  props:
    calculator:
      class: rest.app
      props:
        client:
          providers: client
          class: rest.client

        server:
          providers: server
          class: rest.server
          builder: falcon
