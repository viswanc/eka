---
# Structure of the MVP.
imports:
  - client
  - server

structure:
  props:
    calculator:
      type: app
      props:
        client:
          providers: client
          type: app.client

        server:
          providers: server
          type: app.server
          builder: 'falcon'
